apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: webchat-dev
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
