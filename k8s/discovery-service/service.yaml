apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  namespace: webchat-dev
  labels:
    app: discovery-service
spec:
  selector:
    app: discovery-service
  ports:
    - name: http
      port: 8761
      targetPort: 8761
  type: ClusterIP
